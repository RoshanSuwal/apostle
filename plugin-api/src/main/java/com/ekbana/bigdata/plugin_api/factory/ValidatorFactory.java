package com.ekbana.bigdata.plugin_api.factory;

public interface ValidatorFactory {
    String identity();
//    Validator build(ApplicationContext applicationContext, ApplicationConfig applicationConfig);
}
